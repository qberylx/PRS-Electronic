@{
    ViewBag.Title = "UtilityBudget";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}


<nav class="navbar navbar-expand navbar-gray navbar-light  " >
    <ul class="navbar-nav">
        <li class="nav-item d-none d-sm-inline-block ">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">Account Expenses Type</button>
                <input type="hidden" name="utTabid" id="utTabid" value="1" />
            }

        </li>
        <li class="nav-item d-none d-sm-inline-block">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">Account Division Type</button>
                <input type="hidden" name="utTabid" id="utTabid" value="2" />
            }
            @*<a href="#" class="nav-link text-light">Account Division Type</a>*@
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">Account Department Type</button>
                <input type="hidden" name="utTabid" id="utTabid" value="3" />
            }
            @*<a href="#" class="nav-link text-light">Account Department Type</a>*@
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">Account CC (Lvl 1)</button>
                <input type="hidden" name="utTabid" id="utTabid" value="4" />
            }
            @*<a href="#" class="nav-link text-light">Account CC (Lvl 1)</a>*@
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">Account CC (Lvl 2)</button>
                <input type="hidden" name="utTabid" id="utTabid" value="5" />
            }
            @*<a href="#" class="nav-link text-light">Account CC (Lvl 2)</a>*@
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            @using (Ajax.BeginForm("utBudgetTab", "Budget", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divBudgetTab" }))
            {
                <button type="submit" class="nav-link text-light btn-secondary">IC Category</button>
                <input type="hidden" name="utTabid" id="utTabid" value="6" />
            }
            
        </li>
        @*<li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Help
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
                <a class="dropdown-item" href="#">FAQ</a>
                <a class="dropdown-item" href="#">Support</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">Contact</a>
            </div>
        </li>*@
    </ul>
</nav>

<div id="divBudgetTab">
    @{Html.RenderAction("utBudgetTab", "Budget" , new {utTabid = 1 }); }
</div>
